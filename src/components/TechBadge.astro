---
const { name, image, invert } = Astro.props;
---

<div class="flex flex-col items-center justify-center gap-y-2 p-4 bg-mantle rounded-md w-24 h-24">
  <img
    src={`/images/tech/${image}`}
    alt={name}
    class={`w-10 h-10 ${invert ? 'invert' : ''}`}
  />
  <span class="text-xs text-center text-zinc-700 dark:text-zinc-400">{name}</span>
</div>

